/**/*
# To avoid custom .bin files being ignored, it's recommended to put them in a different directory, perhaps named "data_new/".
# Otherwise, you can also list them here starting with !, for example:
# !/Tiles_MyCustomEnemy.bin
